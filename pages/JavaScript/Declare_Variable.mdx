## 변수 (Var, Let, Const)

> var, let, const 키워드를 사용해서 변수를 선언하고, 변수 선언이라 함은 변수를 생성하는 것을 만한다.
> 변수 생성은 값을 저장하기 위한 공간을 확보하고, 변수 이름과 확보된 메모리 공간의 주소를 연결하는 것이다.

#### ES6부터 var 키워드의 단점을 보완하기 위해 let, const 키워드가 도입되었다.
